#!/bin/bash
set -euxo pipefail

target=$XDG_CONFIG_HOME/dotfiles

tools/git-clone https://github.com/itchyny/dotfiles "$target"

for f in .vimrc .vimrc.yaml .zshrc .tmux.conf; do
  tools/make-symlink "$target/$f" ~/"$f"
done

mkdir -p ~/.vim/cache/swap
